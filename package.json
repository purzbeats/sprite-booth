{
  "name": "sprite-booth",
  "version": "2.0.0",
  "description": "Photo booth app that turns you into an animated pixel art sprite sheet",
  "private": true,
  "scripts": {
    "dev:site": "npx wrangler pages dev site",
    "dev:worker": "cd worker && npx wrangler dev",
    "deploy:site": "npx wrangler pages deploy site --project-name sprite-booth",
    "deploy:worker": "cd worker && npx wrangler deploy"
  }
}
